# --- Core (needed to run the skeleton + CLI in --dry-run) ---
requests>=2.31
PyYAML>=6.0
pysubs2>=1.6          # read/parse .srt/.ass subtitle files
tenacity>=8           # retry/backoff for the service clients

# --- Web server (doblarr serve: UI + library API) ---
fastapi>=0.110
uvicorn>=0.27
pydantic>=2           # request-body models + config schema validation

# --- Real-dub deps (only needed when running real stages) ---
# Installed as an extra: pip install doblarr[real]
# (kept out of the base install because torch is large and GPU-specific —
#  install a torch build matching your platform first, from https://pytorch.org)
# anthropic>=0.40      # Claude-based translation
# prompture>=1.1       # multi-provider LLM translation (incl. local APIs)
# torch                # match your CUDA build from https://pytorch.org
# demucs>=4.0          # music/vocal source separation
# whisperx             # transcription + word-level alignment
# pyannote.audio>=3.1  # speaker diarization (needs a HuggingFace token)

# --- Note ---
# TTS + voice cloning are NOT installed here — Doblarr calls the voicebox
# service over HTTP instead (see doblarr/clients/voicebox.py).
